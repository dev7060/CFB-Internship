#include <stdio.h>

int main()
{
    int r = 3;
    float pi = 3.14;
    printf("area is: %f\n", pi * r * r);
    int height = 3;
    printf("volume of cylinder is %f", pi * r * r * height);
    return 0;
}